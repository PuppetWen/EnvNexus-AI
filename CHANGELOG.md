# EnvNexus AI 更新日志

本文档记录面向用户的主要变化。版本号遵循递增发布顺序。

## 0.1.4 — 2026-07-31

### 全机环境识别

- 扫描范围扩展为所有本地固定磁盘，不再依赖固定用户名、桌面目录或预设工具根目录。
- 自动识别工具可执行文件和遗留的 EnvNexus AI 安装清单，并归入对应工具页面。
- Python、Java、Go、Rust、Node.js、Git、Android 工具链等本机版本按照数字版本倒序排列。
- 清理 Windows `\\?\` 内部路径前缀；旧扫描快照在读取时也会自动规范化。
- Android NDK 从 `source.properties` 读取真实版本，不再把 `ndk-build` 内部 Make 版本误认为 NDK 版本。
- 相同安装根目录按根路径去重，减少 Git 等工具的重复版本项。

### 性能与资源占用

- 新增工具版本指纹缓存。增量刷新仅重新运行新增或发生变化的工具，手动全机扫描仍强制验证全部候选。
- 使用最新注册表 PATH 计算默认版本，取消为每个工具反复启动 `where.exe`。
- 相同配置根目录合并扫描，避免 Android 共享目录被多个组件重复遍历。
- 新增 `env-refresh`，用于不重新遍历全部磁盘的快速验证。
- 窗口失焦或进入托盘时启用 WebView2 低内存目标；窗口恢复后自动切回正常目标，后台任务保持运行。
- 开发机实测：同一全机索引的首次增量探测约 33.2 秒，缓存后约 2.4 秒，结果均为 15 类工具、161 个安装版本；当前 PATH 默认工具仍会实时复核。
- 同口径最小化/失焦测试中，整个 7 进程树的平均工作集由 0.1.3 的 436.1 MiB 降至 0.1.4 的 101.6 MiB，下降约 76.7%；私有提交量基本不变，后台下载和更新能力未暂停。

### 界面与后台任务

- 下载和安装任务保持后台运行，进度区域随窗口大小自适应。
- 更新状态提示移动到侧边栏外侧，避免遮挡菜单。
- 完整版本列表使用内置滚动区域并保持数字版本倒序。

### 稳定性

- 安装、切换、修复和卸载成功后立即刷新本机状态。
- 下载安装支持断点续传和自动重试。
- 校验失败拒绝安装；更新失败保留旧版本。
- 安装版静默覆盖原目录，便携版原路径事务替换，新版本启动后清理更新包。

### 文档与验证

- 新增独立的简体中文 README。
- 更新扫描、性能、命令、构建与发布文档。
- 命令脚本增加到 110 个，新增 `env-refresh.cmd`。
- 自动化验证覆盖前端、Rust、扫描缓存、路径规范化、版本排序、托盘低内存和更新事务。

## 0.1.3 — 2026-07-31

- 完成安装版与便携版的事务自动更新。
- 支持续传、重试、签名和 SHA-256 校验、失败回滚及启动后清理。
- 增加关闭到托盘、版本提示点和更新设置状态。

## 0.1.2 — 2026-07-31

- 增加 GitHub Release 更新检查和发布流程。
- 改进下载稳定性、后台进度和版本清单展示。

## 0.1.1 — 2026-07-26

- 修正便携模式的数据目录与 WebView2 用户数据路径。
- 完善安装位置、路径显示和基础打包流程。
